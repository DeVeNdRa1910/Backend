<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
        <script src="https://cdn.tailwindcss.com"></script>
    </head>

    <body class="bg-zinc-900 text-white">
        <div class="my-4 mx-5">
            <div class="flex justify-between w-full">
                <div class="w-[80%]">
                    <h3>Edit your post</h3>
                    <form action="/update/<%= post._id %>" method="post">
                        <textarea class="w-1/3 resize-none bg-transparent p-3 border-2 border-zinc-800 roundede-md block"  placeholder="What on your mind ?" name="updatedContent" id=""> <%= post.content %> </textarea>
                        <input class="px-3 w-40 py-2 bg-yellow-500 text-md block rounded-md mt-4 text-black text-center"  type="submit" value="Update Post">
                    </form>
                </div>
                <div class="w-[12%]">
                    <a class="bg-red-500 px-10 py-2 inline-block mb-2 rounded-md font-bold" href="/logout">LOGOUT</a>
                </div>
            </div>
        </div>
    </body>

</html>